{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ucp.dev/schemas/requirements-catalog-v1.json",
  "title": "UCP Requirements Catalog",
  "description": "A machine-readable catalog of the normative obligations stated in a UCP capability's specification prose. Produced by scripts/extract_requirements.py. Each record carries two identifiers: a readable positional 'id' for binding tests to, and a 'content_digest' derived from the clause's content alone. Comparing both is what distinguishes a requirement that was reworded from one that was removed.",

  "type": "object",
  "required": [
    "schema_version",
    "extractor_version",
    "spec_version",
    "source_spec",
    "summary",
    "requirements"
  ],
  "additionalProperties": false,

  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri",
      "description": "Identifier of this schema."
    },
    "schema_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
      "description": "Semantic version of the catalog format."
    },
    "extractor_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
      "description": "Version of the tool that produced the catalog."
    },
    "spec_version": {
      "type": "string",
      "description": "UCP release the specification tree represents, such as 'draft' or a dated release."
    },
    "generated_at": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$",
      "description": "UTC timestamp of the run. This is the only field that differs between two runs over identical input, so consumers comparing catalogs for drift must exclude it; --check does."
    },
    "commit_sha": {
      "type": ["string", "null"],
      "pattern": "^[0-9a-f]{40}$",
      "description": "Commit the catalog was generated from, or null outside a git checkout."
    },
    "source_spec": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Repository-relative directories scanned, POSIX separators."
    },
    "summary": { "$ref": "#/$defs/summary" },
    "requirements": {
      "type": "array",
      "items": { "$ref": "#/$defs/requirement" },
      "description": "The requirements, in source order."
    }
  },

  "$defs": {
    "level": {
      "type": "string",
      "enum": [
        "MUST",
        "MUST NOT",
        "SHALL",
        "SHALL NOT",
        "SHOULD",
        "SHOULD NOT",
        "MAY"
      ],
      "description": "RFC 2119 obligation level."
    },

    "summary": {
      "type": "object",
      "required": ["total", "must_count", "should_count", "may_count"],
      "additionalProperties": true,
      "properties": {
        "total": { "type": "integer", "minimum": 0 },
        "must_count": { "type": "integer", "minimum": 0 },
        "must_not_count": { "type": "integer", "minimum": 0 },
        "should_count": { "type": "integer", "minimum": 0 },
        "should_not_count": { "type": "integer", "minimum": 0 },
        "may_count": { "type": "integer", "minimum": 0 },
        "by_level": {
          "type": "object",
          "additionalProperties": { "type": "integer", "minimum": 0 }
        },
        "by_actor": {
          "type": "object",
          "additionalProperties": { "type": "integer", "minimum": 0 }
        },
        "by_file": {
          "type": "object",
          "additionalProperties": { "type": "integer", "minimum": 0 }
        },
        "with_condition": { "type": "integer", "minimum": 0 },
        "with_referenced_fields": { "type": "integer", "minimum": 0 },
        "from_compound_split": { "type": "integer", "minimum": 0 },
        "diagnostics": {
          "type": "object",
          "additionalProperties": { "type": "integer", "minimum": 0 },
          "description": "Counts per diagnostic bucket. Every clause citing an RFC 2119 keyword either reaches the catalog or appears in one of these, so the totals reconcile."
        }
      }
    },

    "requirement": {
      "type": "object",
      "required": [
        "id",
        "content_digest",
        "capability",
        "version",
        "level",
        "clause",
        "source"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^REQ-[A-Z0-9-]+-[0-9]{2,}$",
          "description": "Readable identifier, REQ-<CAPABILITY>-[<DOCUMENT>-]<SECTION>-<ORDINAL>. The ordinal is positional and renumbers when a requirement is inserted above it, which is why content_digest exists."
        },
        "content_digest": {
          "type": "string",
          "pattern": "^[0-9a-f]{10}$",
          "description": "Leading hex of the SHA-256 of capability, normalized compound_parent and normalized text, joined by U+001F. Independent of location, so it survives the document being reorganized."
        },
        "capability": {
          "type": ["string", "null"],
          "description": "Reverse-DNS capability identifier, or null when unresolved."
        },
        "version": {
          "type": "string",
          "description": "Spec release this requirement was read from."
        },
        "level": { "$ref": "#/$defs/level" },
        "target_actor": {
          "type": ["string", "null"],
          "description": "Uppercase party bound by the requirement, following the specification's own vocabulary (BUSINESS, not MERCHANT). Null when no actor could be attributed; guessing was measured and rejected as worse than admitting ignorance.",
          "examples": ["BUSINESS", "PLATFORM", "HOST", "BUYER"]
        },
        "actor_confidence": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Confidence of the strategy that resolved target_actor. 0 when unresolved."
        },
        "clause": {
          "type": "string",
          "minLength": 1,
          "description": "The obligation as written, Markdown emphasis intact."
        },
        "normalized": {
          "type": "string",
          "description": "Canonical form of the clause, used as digest input."
        },
        "condition": {
          "type": ["string", "null"],
          "description": "Leading conditional the obligation is scoped by, verbatim, or null."
        },
        "referenced_fields": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Schema field names the clause names, deduplicated and sorted."
        },
        "compound_parent": {
          "type": ["string", "null"],
          "description": "List stem or matrix context the clause inherits meaning from. A matrix cell reading only 'must' means nothing without it."
        },
        "document_order": {
          "type": "integer",
          "minimum": 0,
          "description": "Position of the source block within its document."
        },
        "source": { "$ref": "#/$defs/source" }
      }
    },

    "source": {
      "type": "object",
      "required": ["file", "start_line", "end_line"],
      "additionalProperties": false,
      "properties": {
        "file": {
          "type": "string",
          "description": "Repository-relative path, POSIX separators."
        },
        "start_line": {
          "type": "integer",
          "minimum": 1,
          "description": "1-based first line of the enclosing block."
        },
        "end_line": {
          "type": "integer",
          "minimum": 1,
          "description": "1-based last line of the enclosing block, inclusive."
        },
        "section": {
          "type": "string",
          "description": "Heading breadcrumb, joined by ' > '."
        },
        "block_type": {
          "type": "string",
          "enum": ["paragraph", "list_item", "table_cell"],
          "description": "Markdown construct the clause was found in."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "Published URL, anchored on the deepest heading. Anchors are produced with the site renderer's own slugify so they resolve against the built documentation."
        }
      }
    }
  }
}
