{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ucp.dev/schemas/shopping/types/error_response.json",
  "title": "Error",
  "description": "Generic error response when business logic prevents resource creation or failed to retrieve resource. Used when no valid resource can be established.",
  "type": "object",
  "required": ["ucp", "messages"],
  "additionalProperties": false,
  "properties": {
    "ucp": {
      "$ref": "../../ucp.json#/$defs/base",
      "description": "UCP protocol metadata."
    },
    "messages": {
      "type": "array",
      "items": {
        "$ref": "message.json"
      },
      "minItems": 1,
      "description": "Array of error messages describing why the operation failed."
    },
    "continue_url": {
      "type": "string",
      "format": "uri",
      "description": "URL for buyer handoff or session recovery."
    }
  }
}
