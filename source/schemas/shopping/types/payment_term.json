{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ucp.dev/schemas/shopping/types/payment_term.json",
  "title": "Payment Term",
  "description": "A buyer's selection of a payment term for specific line items.",
  "type": "object",
  "required": ["id", "line_item_ids"],
  "properties": {
    "id": {
      "type": "string",
      "description": "ID of the selected payment term from available_terms."
    },
    "line_item_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "IDs of line items to apply this term to. Must be a subset of the term's line_item_ids."
    }
  }
}
